<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <base href="<%= base_url %>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= basics.name %> - Portfolio</title>
    <link rel="stylesheet" href="styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>♠️</text></svg>">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-50 text-slate-800 transition-colors duration-300 dark:bg-slate-950 dark:text-slate-300">

    <nav class="sticky top-0 z-50 backdrop-blur-md bg-white/70 border-b border-gray-200 dark:bg-slate-950/70 dark:border-slate-800">
        <div class="max-w-4xl mx-auto px-6 h-16 flex justify-between items-center">
            <span class="font-bold text-xl tracking-tight dark:text-white">My Portfolio</span>
            <button id="theme-toggle" class="p-2 rounded-lg bg-gray-100 dark:bg-slate-800 hover:bg-gray-200 dark:hover:bg-slate-700 transition-all text-xl">
                🌙
            </button>
        </div>
    </nav>

    <header class="max-w-4xl mx-auto py-20 px-6 text-center md:text-left">
        <p class="text-blue-600 dark:text-blue-400 font-medium mb-2 tracking-wide uppercase text-sm">Hi, my name is</p>
        <h1 class="text-5xl md:text-7xl font-extrabold text-slate-900 dark:text-white mb-4 tracking-tight">
            <%= basics.name %>.
        </h1>
        <h2 class="text-4xl md:text-5xl font-bold text-slate-500 dark:text-slate-400 mb-6">
            <%= basics.label %>
        </h2>
        <p class="max-w-xl text-lg text-slate-600 dark:text-slate-400 leading-relaxed mb-8">
            I build systems that are easy to extend and hard to break.<br>
            I transition code from <b>it works</b> to <b>it scales</b><br>
        </p>
        <div class="inline-block h-1 w-20 bg-blue-600 rounded-full"></div>
    </header>

    <main class="max-w-4xl mx-auto px-6 pb-20 space-y-24">

        <section>
            <h2 class="flex items-center text-2xl font-bold text-slate-900 dark:text-white mb-8">
                <span class="text-blue-600 dark:text-blue-400 mr-2">01.</span> Experience
                <span class="ml-4 h-px bg-slate-200 dark:bg-slate-700 flex-grow max-w-xs"></span>
            </h2>

            <div class="space-y-12 border-l-2 border-slate-200 dark:border-slate-800 ml-3 pl-8 relative">
                <% work.forEach(function(job) { %>
                    <div class="relative group">
                        <div class="absolute -left-[39px] top-1 h-5 w-5 rounded-full border-4 border-white dark:border-slate-950 bg-blue-600 dark:bg-blue-500"></div>
                        
                        <div class="flex flex-col sm:flex-row sm:items-baseline sm:justify-between mb-2">
                            <h3 class="text-xl font-bold text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                                <%= job.position %> 
                                <span class="text-blue-600 dark:text-blue-400">@ <%= job.company %></span>
                            </h3>
                            <span class="text-sm font-mono text-slate-500 dark:text-slate-400 whitespace-nowrap mt-1 sm:mt-0">
                                <%= job.startDate %> – <%= job.endDate %>
                            </span>
                        </div>
                        
                        <ul class="list-disc list-outside ml-4 space-y-2 text-slate-600 dark:text-slate-400 leading-relaxed">
                            <% job.highlights.forEach(function(highlight) { %>
                                <li class="pl-2"><%= highlight %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% }); %>
            </div>
        </section>

        <section>
            <h2 class="flex items-center text-2xl font-bold text-slate-900 dark:text-white mb-8">
                <span class="text-blue-600 dark:text-blue-400 mr-2">02.</span> Projects
                <span class="ml-4 h-px bg-slate-200 dark:bg-slate-700 flex-grow max-w-xs"></span>
            </h2>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <% projects.forEach(function(project) { %>
                    <a href="<%= project.url %>" class="group block p-6 bg-white dark:bg-slate-900 rounded-xl shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 border border-slate-100 dark:border-slate-800">
                        <div class="flex justify-between items-start mb-4">
                            <h3 class="text-xl font-bold text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                                <%= project.name %>
                            </h3>
                            <span class="text-slate-400 dark:text-slate-500 group-hover:text-blue-600 dark:group-hover:text-blue-400">
                                ↗
                            </span>
                        </div>
                        <p class="text-slate-600 dark:text-slate-400 mb-6 text-sm leading-relaxed">
                            <%= project.description %>
                        </p>
                        <div class="flex flex-wrap gap-2 mt-auto">
                            <% project.keywords.forEach(function(keyword) { %>
                                <span class="text-xs font-mono text-blue-700 bg-blue-50 dark:text-blue-300 dark:bg-blue-900/30 px-2 py-1 rounded">
                                    <%= keyword %>
                                </span>
                            <% }); %>
                        </div>
                    </a>
                <% }); %>
            </div>
        </section>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
            <section>
                <h2 class="flex items-center text-2xl font-bold text-slate-900 dark:text-white mb-6">
                    <span class="text-blue-600 dark:text-blue-400 mr-2">03.</span> Education
                </h2>
                <div class="space-y-6">
                    <% education.forEach(function(edu) { %>
                        <div class="border-l-2 border-blue-600 pl-4">
                            <h3 class="text-lg font-bold text-slate-900 dark:text-white"><%= edu.institution %></h3>
                            <p class="text-slate-600 dark:text-slate-400 text-sm"><%= edu.studyType %> in <%= edu.area %></p>
                            <p class="text-slate-500 dark:text-slate-500 text-xs mt-1 font-mono">
                                <%= edu.startDate %> – <%= edu.endDate %>
                            </p>
                            <p class="text-blue-600 dark:text-blue-400 text-sm font-bold mt-2">CGPA: <%= edu.score %></p>
                        </div>
                    <% }); %>
                </div>
            </section>

            <section>
                <h2 class="flex items-center text-2xl font-bold text-slate-900 dark:text-white mb-6">
                    <span class="text-blue-600 dark:text-blue-400 mr-2">04.</span> Skills
                </h2>
                <div class="space-y-6">
                    <% skills.forEach(function(category) { %>
                        <div>
                            <h4 class="text-sm font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400 mb-3">
                                <%= category.name %>
                            </h4>
                            <div class="flex flex-wrap gap-2">
                                <% category.keywords.forEach(function(keyword) { %>
                                    <span class="px-3 py-1 text-sm bg-slate-200 dark:bg-slate-800 text-slate-700 dark:text-slate-300 rounded-md border border-slate-300 dark:border-slate-700 hover:border-blue-500 dark:hover:border-blue-400 transition-colors cursor-default">
                                        <%= keyword %>
                                    </span>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>
        </div>

    </main>

    <footer class="text-center py-8 text-slate-500 dark:text-slate-600 text-sm font-mono">
        <p>Designed & Built by <%= basics.name %></p>
    </footer>

    <script src="script.js"></script>
</body>
</html>